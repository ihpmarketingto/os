import { ModulePlaceholder } from "@/components/shell/module-placeholder";

export default function Page() {
  return (
    <ModulePlaceholder
      title="CRM"
      phase={1}
      description="Full pipeline management, leads, deals, contacts and lead scoring land in Phase 1."
    />
  );
}
